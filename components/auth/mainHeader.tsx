"use client";

import Navbar from "@/components/auth/navbar"; // Adjust this import path according to your structure

const Header = () => {
  return (
    <header className="bg-gray-900 text-white">
      <Navbar />
    </header>
  );
};

export default Header;
